'use client';

import ExamInfoView from '@/components/examinfo/ExamInfoView';

export default function ExamInfoPage() {
    return (
        <main className="flex-1 flex flex-col min-h-0 bg-white dark:bg-black">
            <ExamInfoView />
        </main>
    );
}
